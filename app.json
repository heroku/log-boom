{
  "name": "Log Boom",
  "description": "A simple ring buffer log collector",
  "repository": "https://github.com/heroku/log-boom",
  "keywords": ["golang", "logplex", "buffer", "drain"],
  "env": {
    "MAGIC_TOKEN": {
      "description": "A secret key for magic link authentication on bootstrap.",
      "generator": "secret"
    },
    "EMAIL": {
      "description": "The email address to send a magic link to.",
      "required": true,
      "value": ""
    }
  }
}
